# Socket.IO server URL (optional).
# For local dev, leave unset so the client uses the same origin (Vite proxy forwards to server).
# For production (e.g. Vercel frontend + Render backend), set to your backend URL, e.g.:
# VITE_SOCKET_URL=https://f1-ph-welcome-game.onrender.com
